language: ruby
rvm:
 - 2.4.2
services:
  - postgresql
script:
  - gem install bundler
  - bundle install
  - export RAILS_ENV=test
  - bundle exec rake db:schema:load
  - bundle exec rspec
  - bundle exec cucumber